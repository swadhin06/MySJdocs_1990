---
- hosts : 172.31.35.101
  become: yes
  tasks:
  - include_tasks : myfile.yml
  - name : install webserver
    apt:
       name : apache2
       state: present
  - name : copying file
    copy:
        src: mytestfile
        dest: /home/ubuntu
